# Clave de firma: NUNCA se versiona. Vive solo como secret en GitHub.
android.keystore
*.keystore
*.jks

# Proyecto Android generado por Bubblewrap a partir de twa-manifest.json.
# Es reconstruible en cada build, no se versiona (regla oficial de Bubblewrap).
app/
build/
.gradle/
gradle/
gradlew
gradlew.bat
build.gradle
settings.gradle
gradle.properties
local.properties
*.iml
.idea/

# Artefactos de compilacion
*.aab
*.apk
*.idsig
app-release-bundle.aab
app-release-signed.apk
app-release-unsigned.apk
app-release-unsigned-aligned.apk

# Credenciales temporales descargadas como respaldo
passwords.txt
